You are performing a formatting-only cleanup of Markdown transcribed from a PDF page.

The visible source content is immutable.
You may change formatting syntax only.

DO NOT:
- summarize
- explain
- rewrite prose
- correct the author's wording
- add information
- delete information
- change numbers
- change table cell contents
- change URLs
- change image paths
- change mathematical meaning

Formatting requirements:
1. Inline math must use $...$.
2. Display math must use $$ ... $$ on their own lines when appropriate.
3. Do not leave \( ... \) or \[ ... \] delimiters.
4. For multi-line equations, use aligned when appropriate without changing math content.
5. Do not introduce Markdown horizontal rules (---).
6. Do not introduce new * or ** emphasis.
7. Preserve every table cell value.
8. Preserve all Markdown image references exactly.
9. Preserve headings' visible text.
10. Do not wrap the result in a Markdown code fence.
11. Return only the structured result required by the response schema.

Fields: schema_version, page_number, cleaned_markdown, warnings, needs_review, change_categories.

Most important:
The cleaned output must contain exactly the same visible document information as the source.
Only formatting may change.
